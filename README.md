# Differential Equations Sandbox

For now, just testing some ODEs and PDEs using forward/backward Euler and graphing solutions/errors.

## Getting Started

```
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Navigate to folders, run the python files, and visualize solutions!